const form : HTMLFormElement | null = document.querySelector("form");

const select : HTMLSelectElement | null  = document.querySelector("select");

const divs : Element | null = document.querySelector("#container");